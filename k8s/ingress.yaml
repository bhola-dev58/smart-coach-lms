apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gradify-academy-ingress
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m" # allow large video/file uploads
    cert-manager.io/cluster-issuer: "letsencrypt-prod" # automatically issue TLS certificates
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - gradify.academy # Replace with your actual domain
      secretName: gradify-tls
  rules:
    - host: gradify.academy # Replace with your actual domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gradify-academy-service
                port:
                  number: 80

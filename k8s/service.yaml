apiVersion: v1
kind: Service
metadata:
  name: meetme-center-service
  labels:
    app: meetme-center
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: meetme-center
